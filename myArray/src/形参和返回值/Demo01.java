package 形参和返回值;
/*
  1.类名作为形参和返回值
        方法的形参是 类名，实际需要的是该类的 对象
        方法的返回值是 类名，实际返回的是该类的 对象

  2.抽象类名作为形参和返回值
        方法的形参是 抽象类名，实际需要的是该抽象类的 子类对象
        方法的返回值是 抽象类名，实际返回的是该抽象类的 子类对象

  3.接口名作为形参和返回值
        方法的形参是 接口名，实际需要的是该接口的 实现类对象
        方法的返回值是 接口名，实际返回的是该接口类的 实现类对象

 */
public class Demo01 {
}
